async function executeSwap(web3, { buyOn, sellOn, priceA, priceB }) {
  console.log(`💱 Swap végrehajtás (szimulált): ${buyOn} → ${sellOn}`);
  // TODO: implementálni az approve + swap logikát, privát kulcs használatával
}

module.exports = { executeSwap };
